# FormulaGen — Intel AI PC (Arc 140V XPU) dependencies
# ---------------------------------------------------------------------------
# STEP 1 — install the Intel XPU build of PyTorch FIRST (this is the part that
# makes the Arc GPU usable; the default `pip install torch` is CPU-only):
#
#     pip uninstall -y torch
#     pip install torch --index-url https://download.pytorch.org/whl/xpu
#
# Verify:  python -c "import torch; print(torch.xpu.is_available())"   -> True
#
# STEP 2 — install the rest:
#     pip install -r requirements.txt
#
# NOTE: bitsandbytes is intentionally NOT here — it is CUDA-only and this
# project runs with NO quantization on the Arc's 16 GB.
# ---------------------------------------------------------------------------

transformers>=4.52.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.10.0
trl>=0.8.6
sentencepiece
protobuf
huggingface_hub
